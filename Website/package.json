{
  "name": "@liveol/website",
  "scripts": {
    "start": "yarn run-p start:serve build:sass:watch",
    "build:sass": "yarn sass assets/scss/style.scss assets/css/style.css",
    "build:sass:watch": "yarn build:sass --watch",
    "start:serve": "yarn serve . -l 8000"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.26.5",
    "serve": "^11.3.0"
  },
  "license": "ISC"
}
